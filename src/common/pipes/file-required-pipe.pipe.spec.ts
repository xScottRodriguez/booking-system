import { FileRequiredPipePipe } from './file-required-pipe.pipe';

describe('FileRequiredPipePipe', () => {
  it('should be defined', () => {
    expect(new FileRequiredPipePipe()).toBeDefined();
  });
});
